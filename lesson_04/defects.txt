test_contains_positive ("Cool", "c")
    Неверный ответ при вызове функции

    Приоритет: высокий
    Окружение: VS Code

    Шаги:
        1. Вызвать функцию test_contains_positive
        2. Прописать символ в противоположном регистре, например "c" для "Cool" или  "H" для "hello"

    ОР: функция сработает с ответом True
    ФР: функция срабатывает с ответом False

test_delete_symbol_positive ("Moon", "O")
    Ошибка при вызове функции

    Приоритет: высокий
    Окружение: VS Code

    Шаги:
        1. Вызвать функцию test_delete_symbol_positive
        2. Прописать символ в противоположном регистре, например "O" для "Moon"

    ОР: функция сработает с ответом Mn
    ФР: функция выдаёт ошибку

    